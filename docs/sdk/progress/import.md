# Progress Bar Module

The **Progress Bar** module acts as a bridge between the Versa SDK and the progress bar that you run on your server.

## Import
* Please Note: The Progress Bar module cannot be imported onto the server

```lua
local Progress = require '@versa_sdk/modules/progress'
```